MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/
DB_NAME=scheduleApp
PORT=3000
CORS_ORIGIN=*

# 選填：PDF 匯入/匯出的隱藏資料加密金鑰。未設定時該功能停用（其他功能不受影響）。
# 必須是 64 字元 hex 字串（32 bytes），可用下列指令產生：
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PDF_PAYLOAD_SECRET=
